/* const btnEl = document.querySelector('.header-tab');
const btnElList = document.querySelectorAll('.header-tab');

btnElList.forEach(btnEl => {
    btnEl.addEventListener('click', () => {
        if (btnEl.style.opacity == '0') { btnEl.style.setProperty('--selected-opacity', '1'); }
        else {
            btnEl.style.setProperty('--selected-opacity', '0'); 
        }
    })
}); */